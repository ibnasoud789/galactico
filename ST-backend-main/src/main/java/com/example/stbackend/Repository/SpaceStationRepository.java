package com.example.stbackend.Repository;

import com.example.stbackend.Model.SpaceStation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SpaceStationRepository extends JpaRepository<SpaceStation, String> {
}
